d={}
for _ in range(int(input())):
    name=input()
    grade=float(input())
    d[name]=grade
v=d.values()
second=sorted(list(set(v)))[1]
second_list=[]
for key,values in d.items():
    if values==second:
        second_list.append(key)
    print(second_list.sort())
for names in second_list:
    print(names)
